#ifndef REFLECTOR_H
#define REFLECTOR_H

#include <iostream>
#include <fstream>
#include <string>
#include "MachineComponents.h"

class Reflector {
	public:
		Reflector();
		int processMapping(int index);
};

#endif
